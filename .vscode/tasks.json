{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"label": "Normie build",
			"command": "/usr/bin/g++",
			"args": [
				"-g",
				"${workspaceFolder}/*.cpp",
				"${workspaceFolder}/*.h",
				"-o",
				"array-stack.out"
			],
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": "build",
			"detail": "compiler: /usr/bin/g++"
		}
	]
}